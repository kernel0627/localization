# 问题 1：圆形无人机编队的纯方位定位与调整

圆形编队的三个子问题可以在同一个角度观测模型下求解：1.1 在发射机位置和编号已知时估计接收位置；1.2 把匿名编号作为离散未知量，与接收位置联合辨识；1.3 在辅助发射机也有偏差的情况下，反复估计本机偏差并执行相对修正。三问依次增加了身份不确定性和参考位置不确定性，因此需要分别交代定位解的选择依据、匿名身份的可辨识条件，以及迭代结果的适用范围。

## 统一坐标、符号与观测约定

各无人机保持同一飞行高度，位置问题可在平面中讨论。取 FY00 为原点，FY00 指向 FY01 的理想方向为横轴正向，圆周半径为已知的 $R$。用 $Q_i$ 表示 FY$i$ 的理想位置，$P_i$ 表示实际位置：

$$
Q_0=(0,0),\qquad
Q_i=R\begin{pmatrix}\cos\theta_i^0\\\sin\theta_i^0\end{pmatrix},\qquad
\theta_i^0=\frac{2\pi(i-1)}9,\quad i=1,\ldots,9.
$$

相邻圆周编号的理想角间隔为 $40^\circ$。1.1、1.2 的公式保留一般半径 $R$，数值检查采用 $R=100\,\mathrm{m}$；1.3 按题目要求固定为 $100\,\mathrm{m}$。已知半径提供长度尺度，否则仅凭夹角只能确定相应的归一化几何。

接收机知道自身编号 $k$，从而知道目标位置 $Q_k$。对任意接收位置 $p$ 和两个发射位置 $A,B$，定义无向视线夹角

$$
H(p;A,B)=\arccos\!\left(
\frac{(A-p)^{\mathsf T}(B-p)}{\lVert A-p\rVert\,\lVert B-p\rVert}
\right).
$$

观测只含两条视线之间的夹角，范围为 $[0,\pi]$，不要求接收机测得相对于横轴的绝对方位。各路信号能够区分，因而每个夹角都保留对应的信号配对关系；计算使用弧度，表格可用角度展示。接收机与发射机重合时视线没有定义，必须排除。使用角度导数和等角圆的局部论证还需避开夹角为 $0$ 或 $\pi$ 的共线退化情况。

以下将理想锚点预测简写为 $h_{uv}(p)=H(p;Q_u,Q_v)$。符号 $\binom{A}{2}$ 表示集合 $A$ 中所有无序二元子集；当元素是编号时，它表示所有发射信号配对。

## 1.1 已知编号与准确发射位置下的定位

### 已知条件与核心模型

本问由 FY00 和两架圆周机 FY$i$、FY$j$ 发射，三者位置无偏差，即实际位置等于 $Q_0,Q_i,Q_j$。接收机 FY$k$ 不属于发射集合，实际位置 $p=(x,y)$ 略偏离 $Q_k$。输入为发射编号、半径、自身编号，以及三组带配对关系的实测夹角；未知量为接收机的两个平面坐标。

令 $\mathcal A_{ij}=\{(0,i),(0,j),(i,j)\}$，本问模型归纳为

$$
\left\{
\begin{aligned}
\alpha_{uv,k}^{\mathrm{obs}}&=h_{uv}(p)+\varepsilon_{uv,k},
&& (u,v)\in\mathcal A_{ij},\\
h_{uv}(p)&=\arccos\!\left(
\frac{(Q_u-p)^{\mathsf T}(Q_v-p)}{\lVert Q_u-p\rVert\,\lVert Q_v-p\rVert}
\right),\\
r_{uv,k}(p)&=h_{uv}(p)-\alpha_{uv,k}^{\mathrm{obs}},\\
p_k^*&\in\underset{p\notin\{Q_0,Q_i,Q_j\}}{\operatorname{argmin}}
\sum_{(u,v)\in\mathcal A_{ij}}r_{uv,k}(p)^2,
&& p^{(0)}=Q_k.
\end{aligned}
\right.
$$

这里的最小二乘式给出待求目标，实际算法从 $Q_k$ 出发求局部解，并不声称遍历全平面求得所有全局最优点。精确测角时 $\varepsilon_{uv,k}=0$；存在噪声时，三个角一般无法同时完全拟合。若各对夹角误差相互独立、同方差且服从零均值高斯分布，最小二乘目标对应该误差假设下的最大似然估计。若夹角由同一组方位测量相减得到，误差可能相关，需要相应调整统计模型。

### 求解、解的选择与局部稳定性

角度方程非线性，采用带解析 Jacobian 的非线性最小二乘求解。记 $a=Q_u-p$、$b=Q_v-p$、$\gamma=(a^{\mathsf T}b)/(\lVert a\rVert\lVert b\rVert)$，在非退化位置有

$$
\nabla_p\gamma=-\frac{a+b}{\lVert a\rVert\lVert b\rVert}
+\gamma\left(\frac{a}{\lVert a\rVert^2}+\frac{b}{\lVert b\rVert^2}\right),
\qquad
\nabla_p h_{uv}=-\frac{\nabla_p\gamma}{\sqrt{1-\gamma^2}}.
$$

把三个角的梯度按行排列得到 $J_\alpha$。求解器利用残差和该矩阵更新位置，并输出估计坐标、求解状态、残差范数、奇异值和条件数。局部误差传播满足 $\delta\boldsymbol\alpha\approx J_\alpha\delta p$。当 $\operatorname{rank}(J_\alpha)=2$ 时，附近两个位置方向均可由角度变化区分；最小奇异值 $\sigma_{\min}(J_\alpha)$ 越小，某个方向上的测角扰动越容易被放大为位置误差。

三组无向夹角并不保证全局唯一。不同位置可能给出同一观测，接收机自身编号及“小幅偏差”先验用于选择靠近 $Q_k$ 的物理解。题目未规定偏差的统一米数上限，1.1 的实现据此使用理想位置作初值，没有额外指定固定半径的可行域。对疑似多解情形，可以用多个初值检查候选；小残差说明观测拟合良好，还需结合位置先验判断采用哪一个解。局部 Jacobian 满秩也只支持局部可辨识性。

### 算例与数值依据

取 FY00、FY01、FY05 发射，FY03 接收，$R=100\,\mathrm{m}$。三个发射点为 $(0,0)$、$(100,0)$ 和约 $(-93.969,34.202)\,\mathrm{m}$。借用表 1 的 FY03 接收位置 $(112\,\mathrm{m},80.21^\circ)$ 生成观测，按 FY00–FY01、FY00–FY05、FY01–FY05 排列，得到约 $46.050^\circ$、$46.231^\circ$、$92.282^\circ$。

从 $Q_3\approx(17.365,98.481)\,\mathrm{m}$ 出发，恢复 $p_3^*\approx(19.044,110.369)\,\mathrm{m}$。多起点检查还得到约 $(-13.616,-76.061)\,\mathrm{m}$ 的另一零残差位置；两者到 $Q_3$ 的距离分别约为 $12.01\,\mathrm{m}$ 和 $177.27\,\mathrm{m}$，前者符合本算例的小偏差情形。上述数值经过展示舍入，计算使用完整精度。

已有验证覆盖全部 $\binom92=36$ 组圆周发射机搭配，每组 7 个合法接收机，共 252 个表 1 接收位置案例；发射机始终使用理想坐标。精确观测下全部恢复生成观测的位置。进一步对每对夹角独立加入标准差 $0.1^\circ$ 的高斯扰动，每例运行 200 次，共 50400 次，得到以下条件性统计：

| 两架圆周发射机的较小圆心角间隔 | 位置误差中位数 / m | 位置误差 P95 / m |
|---:|---:|---:|
| $40^\circ$ | 0.49 | 1.41 |
| $80^\circ$ | 0.31 | 0.79 |
| $120^\circ$ | 0.25 | 0.64 |
| $160^\circ$ | 0.23 | 0.59 |

P95 为位置欧氏误差的 95% 分位数。在这些编队位置和噪声条件下，更大的圆心角间隔对应更小的含噪定位误差，与局部奇异值的改善一致。这一结果说明参考几何会影响稳定性，为 1.3 的参考组合评分提供依据；实际传感器精度仍取决于其误差分布与相关性。

## 1.2 匿名发射机的最少数量与身份定位

### 已知条件与联合模型

本问 FY00、FY01 的身份已知，其他信号的编号未知，各发射机仍位于理想位置。接收机知道自身编号 $k\in\{2,\ldots,9\}$、理想位置和半径。先考察只增加一路匿名信号 $U$ 的情形：三路信号可分别识别为 FY00、FY01、$U$，故 $\alpha_{01}$、$\alpha_{0U}$、$\alpha_{1U}$ 的配对明确；待求量为匿名编号 $q$ 和接收位置 $p$。

给定某个 $q$ 后，1.1 已能计算位置。因此，枚举合法编号，并在各编号下保留满足全部精确角度约束的位置，再按自身理想位置筛选。本问核心模型为

$$
\left\{
\begin{aligned}
q&\in\mathcal Q_k=\{2,\ldots,9\}\setminus\{k\},\\
f_q(p)&=\big(h_{01}(p),h_{0q}(p),h_{1q}(p)\big)^{\mathsf T},\\
\boldsymbol\alpha_k^{\mathrm{obs}}
&=\big(\alpha_{01,k}^{\mathrm{obs}},\alpha_{0U,k}^{\mathrm{obs}},
\alpha_{1U,k}^{\mathrm{obs}}\big)^{\mathsf T}=f_q(p),\\
\mathcal C_q&=\{p\notin\{Q_0,Q_1,Q_q\}:f_q(p)=\boldsymbol\alpha_k^{\mathrm{obs}}\},\\
(q^*,p_k^*)&\in\underset{q\in\mathcal Q_k,\;p\in\mathcal C_q}{\operatorname{argmin}}
\lVert p-Q_k\rVert,\\
m_{\min}&=1\qquad\text{（精确测角、已知自身编号与尺度、偏差充分小时）}.
\end{aligned}
\right.
$$

其中 $m_{\min}$ 计数的是 FY00、FY01 之外的额外发射机。数学上的候选集合采用精确相等，程序通过三角残差范数小于 $10^{-8}\,\mathrm{rad}$ 的数值检验实现。距离并列或连续退化分支可能影响最近结果时，程序报告不能唯一确定。

### 额外 0 架为什么不足

仅有 FY00、FY01 时，本机只能得到 $\alpha_{01}$。固定两个端点和一个非退化夹角，接收位置落在通过这两个端点的等角圆弧上。接收机沿自身理想位置附近的同一圆弧作任意小移动，观测保持不变；即使已知自身编号且偏差很小，仍存在一维连续候选。因此额外 0 架无法唯一确定平面位置，至少需要增加 1 架。

### 额外 1 架的自含局部充分性论证

需要同时证明匿名编号能够区分、正确编号下的位置能够确定。

首先固定接收机在 $Q_k$。令 $u_0,u_1$ 为该点指向 $Q_0,Q_1$ 的单位向量。九等分圆周不存在与 $Q_1$ 直径相对的圆周编号，且 $k\ne1$，所以 $u_0,u_1$ 不共线。若匿名方向的单位向量为 $w$，两个观测给出 $u_0^{\mathsf T}w=\cos\alpha_{0U}$ 与 $u_1^{\mathsf T}w=\cos\alpha_{1U}$。这两个线性约束的系数矩阵可逆，因而确定唯一的 $w$。从圆周点 $Q_k$ 沿已确定的射线，与编队圆最多再交于一个其他点，故确定唯一匿名编号。

于是，有限个角度特征 $f_q(Q_k)$ 两两不同，最小间隔

$$
d_k=\min_{q\ne q',\;q,q'\in\mathcal Q_k}
\lVert f_q(Q_k)-f_{q'}(Q_k)\rVert>0.
$$

利用所有 $f_q$ 在 $Q_k$ 附近的连续性，可以选取共同邻域 $B_k$，使每个编号、每个 $p\in B_k$ 都有 $\lVert f_q(p)-f_q(Q_k)\rVert<d_k/3$。对不同编号 $q,q'$ 及邻域内任意 $p,p'$，三角不等式给出 $\lVert f_q(p)-f_{q'}(p')\rVert>d_k/3$。因此不同编号即使采用不同的附近接收位置，也无法产生相同观测。

其次，在正确编号 $q$ 下，$\alpha_{01}$ 与 $\alpha_{0U}$ 分别限定通过 $Q_0,Q_1$ 和通过 $Q_0,Q_q$ 的等角圆弧。在理想接收位置，这两个圆均经过 $Q_k$ 与 $Q_0$，且不可能重合：否则同一个圆将包含三个不同的圆周点 $Q_1,Q_q,Q_k$，故必须等于编队圆，同时又通过圆心 $Q_0$，产生矛盾。两个不同的圆在 $Q_0,Q_k$ 有两个不同交点，故在 $Q_k$ 处横截相交。足够小的扰动保持非退化和局部分支不变，两条约束圆弧附近只有一个合法交点，公共发射点 $Q_0$ 则被排除。

将身份分离与位置唯一性的邻域缩小为以 $Q_k$ 为中心的同一小球，其中只有真实编号和真实位置这一组解。真实位置处于该球内时，比球外任何候选更接近 $Q_k$，因此最近候选规则选中真实解。结合数量下界，可得精确测角且偏差充分小时 $m_{\min}=1$。这个证明给出局部邻域的存在性，没有给出统一的偏差米数上限，也未覆盖任意大偏差或含噪身份辨识。

### 候选的计算与退化处理

每个接收机有 7 个合法匿名编号。对每个编号，先从 $Q_k$ 调用 1.1 的定位求解，再通过等角圆交点补查可能遗漏的解。这样，某一次局部求解失败不会直接被解释为该编号无解。

具体地，将坐标除以 $R$，令 $a_1=Q_1/R$、$a_q=Q_q/R$，以 $\mathcal J(x,y)=(-y,x)$ 表示逆时针旋转 $90^\circ$。对于端点 $0,a$ 和夹角 $\alpha\in(0,\pi)$，两个可能的圆心为

$$
C_s(a,\alpha)=\frac{a+s\cot\alpha\,\mathcal Ja}{2},\qquad s\in\{-1,+1\}.
$$

这些圆经过原点，方程为 $\lVert x\rVert^2-2C_s^{\mathsf T}x=0$。分别用 $\alpha_{01}$、$\alpha_{0U}$ 建圆，得到四组圆心配对 $C=C_s(a_1,\alpha_{01})$、$D=C_t(a_q,\alpha_{0U})$。当 $C\ne D$ 时，两圆相减给出 $(C-D)^{\mathsf T}x=0$；令 $v=\mathcal J(C-D)/\lVert C-D\rVert$，除原点外的候选为 $x=2(C^{\mathsf T}v)v$，乘以 $R$ 恢复实际尺度。

整圆还包含可能对应补角的圆弧，因此每个交点必须重新检验全部三个原始无向角，并剔除发射点、合并重复点。若 $C=D$，约束可能允许连续圆弧。此时保存该整圆到 $Q_k$ 的距离下界 $b=R\left|\lVert Q_k/R-C\rVert-\lVert C\rVert\right|$；只有最佳离散候选严格比所有这类下界更近，才能排除连续分支的影响，否则报告退化。这个检验对连续分支采取保守处理。

### 算例与验证范围

继续使用 1.1 的 FY03 接收算例，将 FY05 的真实编号隐藏，只输入三组夹角、接收机编号和半径。7 个编号都能给出角度一致的位置，距离筛选结果如下：

| 假设匿名编号 | 该编号最近候选到 $Q_3$ 的距离 / m |
|:---|---:|
| FY05 | 12.01 |
| FY06 | 38.24 |
| FY04 | 51.46 |
| FY07 | 77.60 |
| FY02 | 115.95 |
| FY09 | 174.49 |
| FY08 | 206.07 |

算法选择 FY05，输出约 $(19.044,110.369)\,\mathrm{m}$ 的接收位置，与观测真值一致。错误编号也有精确拟合解，说明位置先验在身份筛选中不可省略。

已有验证遍历 8 个接收机与各自 7 个匿名编号：理想接收位置 56 例、表 1 接收位置 56 例，以及径向偏差 $\{-5,0,5\}\,\mathrm{m}$ 与角向偏差 $\{-1,0,1\}^\circ$ 的非零组合共 448 例，合计 560 例。全部编号正确，位置误差均小于 $10^{-6}\,\mathrm{m}$，最大回代误差约为 $1.05\times10^{-9}\,\mathrm{m}$。真实编号和坐标只用于生成观测与事后评价。

这些结果验证了精确几何下的实现；所选扰动点并不构成整个连续区域的保证。额外一架的数量结论由前述局部几何论证支撑，有限案例用于检查枚举、候选补查和选择过程。

## 1.3 公开轮换下的本机双偏差迭代调整

### 表 1 条件、状态和控制信息

本问每次允许 FY00 与最多 3 架圆周机发射。表 1 给定 FY00 已在圆心，FY01 已在半径 $100\,\mathrm{m}$、方位 $0^\circ$ 的基准位置。本算例固定这两架作为已标定参考，由 FY02—FY09 调整队形。固定标定是该算例采用的条件；若两架基准也有未知偏差，需要另行处理基准和尺度。

| 无人机 | 初始半径 / m | 初始方位 / ° | 目标方位 / ° |
|:---|---:|---:|---:|
| FY00 | 0 | 0 | 圆心 |
| FY01 | 100 | 0 | 0 |
| FY02 | 98 | 40.10 | 40 |
| FY03 | 112 | 80.21 | 80 |
| FY04 | 105 | 119.75 | 120 |
| FY05 | 98 | 159.86 | 160 |
| FY06 | 112 | 199.96 | 200 |
| FY07 | 105 | 240.07 | 240 |
| FY08 | 98 | 280.17 | 280 |
| FY09 | 112 | 320.28 | 320 |

圆周机目标半径均为 $R=100\,\mathrm{m}$。以 $P_k(t)$ 表示完成 $t$ 个测角时隙后的实际位置，定义实际径向偏差 $\delta r_k(t)$ 和角向偏差 $\delta\theta_k(t)$：

$$
P_k(t)=\big(R+\delta r_k(t)\big)
\begin{pmatrix}
\cos\big(\theta_k^0+\delta\theta_k(t)\big)\\
\sin\big(\theta_k^0+\delta\theta_k(t)\big)
\end{pmatrix}.
$$

这两个实际偏差用于描述动力学和离线评价。本机控制只输入自身测得的夹角，并利用预先已知的自身编号、目标队形、公开时隙与发射编号；持久记忆只有自身连续达标计数和保持标志。参考评分在本机计算，没有评分交换，也不使用其他接收机的观测或实际坐标。

执行层假定能感知指向 FY00 的方向，并按统一旋转正方向精确完成相对径向位移和绕 FY00 的相对角位移。控制器输出两个相对指令；实际极坐标由仿真器用于模拟执行。精确相对极坐标运动是本次仿真的执行假设。

### 28 时隙公开发射表

从 $\{2,\ldots,9\}$ 中按字典序枚举所有辅助对 $(a,b)$，每时隙由 FY00、FY01、FY$a$、FY$b$ 发射，其余六架接收。一个完整周期包含 $\binom82=28$ 个时隙，每架待调整机发射 7 次、接收 21 次。下表中的数字表示 FY 编号；每行均另含固定发射机 FY00、FY01。

| 周期内时隙 | 辅助发射对 | 接收编号 |
|---:|:---:|:---|
| 1 | 02、03 | 04、05、06、07、08、09 |
| 2 | 02、04 | 03、05、06、07、08、09 |
| 3 | 02、05 | 03、04、06、07、08、09 |
| 4 | 02、06 | 03、04、05、07、08、09 |
| 5 | 02、07 | 03、04、05、06、08、09 |
| 6 | 02、08 | 03、04、05、06、07、09 |
| 7 | 02、09 | 03、04、05、06、07、08 |
| 8 | 03、04 | 02、05、06、07、08、09 |
| 9 | 03、05 | 02、04、06、07、08、09 |
| 10 | 03、06 | 02、04、05、07、08、09 |
| 11 | 03、07 | 02、04、05、06、08、09 |
| 12 | 03、08 | 02、04、05、06、07、09 |
| 13 | 03、09 | 02、04、05、06、07、08 |
| 14 | 04、05 | 02、03、06、07、08、09 |
| 15 | 04、06 | 02、03、05、07、08、09 |
| 16 | 04、07 | 02、03、05、06、08、09 |
| 17 | 04、08 | 02、03、05、06、07、09 |
| 18 | 04、09 | 02、03、05、06、07、08 |
| 19 | 05、06 | 02、03、04、07、08、09 |
| 20 | 05、07 | 02、03、04、06、08、09 |
| 21 | 05、08 | 02、03、04、06、07、09 |
| 22 | 05、09 | 02、03、04、06、07、08 |
| 23 | 06、07 | 02、03、04、05、08、09 |
| 24 | 06、08 | 02、03、04、05、07、09 |
| 25 | 06、09 | 02、03、04、05、07、08 |
| 26 | 07、08 | 02、03、04、05、06、09 |
| 27 | 07、09 | 02、03、04、05、06、08 |
| 28 | 08、09 | 02、03、04、05、06、07 |

发射机在本时隙保持位置。一次测角时隙依次完成“冻结当前队形并测角—六架接收机独立计算—同时执行六组相对指令”，下一时隙重新测量。$t=0$ 为表 1 初态，第 $t$ 个时隙使用 $P(t-1)$ 生成观测，执行后得到 $P(t)$。四架同时发射计为一个时隙，六架同时调整也包含在该时隙内；进入保持后继续测角而不移动的过程仍计入时隙。题目没有给定测角持续时间，累计时隙衡量操作次数。

公开表直接确定各路发射编号，因此无需在每个时隙重新执行 1.2 的匿名枚举。与前两问相比，辅助发射机 FY$a$、FY$b$ 仍可能有偏差：本机测得的角由它们的实际位置形成，而用于预测的锚点仍是理想位置。这种模型差异会传入本机估计，是反复轮换和小步修正需要面对的耦合因素。

### 本机估计、择优与运动的核心模型

在第 $t$ 个时隙，记发射集合为 $T_t=\{0,1,a,b\}$，圆周发射集合为 $C_t=\{1,a,b\}$。每架接收机获得 $\binom42=6$ 个夹角，从三个圆周发射机中选取参考对 $S\in\binom{C_t}{2}$，即 $\{1,a\}$、$\{1,b\}$、$\{a,b\}$。每个参考对与 FY00 组成一次三锚点定位。

为统一数值尺度，采用 $z=(z_r,z_t)=(\widehat{\delta r},R\widehat{\delta\theta})$，两项单位均为米，并令

$$
p_k(z)=(R+z_r)
\begin{pmatrix}\cos(\theta_k^0+z_t/R)\\\sin(\theta_k^0+z_t/R)\end{pmatrix}.
$$

将成功求得局部解并通过角度一致性检验的参考对组成集合 $\mathcal E_k(t)$。在暂省本时隙估计量的时间标记后，本机模型归纳如下，其中最后两行为尚未保持时的运动规则：

$$
\left\{
\begin{aligned}
\alpha_{uv,k}^{\mathrm{obs}}(t)
&=H\big(P_k(t-1);P_u(t-1),P_v(t-1)\big),
&&\{u,v\}\in\binom{T_t}{2},\\
z_{k,S}^*&\in\underset{z}{\operatorname{argmin}}
\sum_{\{u,v\}\in\binom{\{0\}\cup S}{2}}
\big[h_{uv}(p_k(z))-\alpha_{uv,k}^{\mathrm{obs}}(t)\big]^2,\\
c_{k,S}&=\left[\sum_{\{u,v\}\in\binom{T_t}{2}}
\big[h_{uv}(p_k(z_{k,S}^*))-\alpha_{uv,k}^{\mathrm{obs}}(t)\big]^2\right]^{1/2},\\
S_k^*&=\underset{S\in\mathcal E_k(t)}{\operatorname{argmax}}
\frac{\sigma_{k,S}}{1+c_{k,S}/s},
&&s=10^{-3}\,\mathrm{rad},\\
\widehat{\delta r}_k&=(z_{k,S_k^*}^*)_r,
\qquad \widehat{\delta\theta}_k=(z_{k,S_k^*}^*)_t/R,\\
\rho_k&=\max\left(
\frac{|\widehat{\delta r}_k|}{\tau_r},
\frac{|\widehat{\delta\theta}_k|}{\tau_\theta},
\frac{c_{k,S_k^*}}{\tau_c}\right),\\
\Delta r_k&=\operatorname{clip}(-\eta\widehat{\delta r}_k,-5\,\mathrm{m},5\,\mathrm{m}),
&&\eta=0.5,\\
\Delta\theta_k&=\operatorname{clip}(-\eta\widehat{\delta\theta}_k,-2^\circ,2^\circ).
\end{aligned}
\right.
$$

其中 $\operatorname{clip}(x,l,u)=\min(\max(x,l),u)$；$\rho_k$ 用于后述本机保持与恢复。观测式为本次精确测角仿真假设。每个候选从 $z=0$ 启动，利用角度 Jacobian 和极坐标变换的链式法则求取附近的最小二乘解。

候选的数值有效性检查与异常处理见文末验证资料。小的三角残差说明候选能解释所选角度；辅助发射机实际位置有偏差时，即使残差很小，本机位置估计也可能有偏差。

### 三参考对为什么要同时比较

对每个合格候选，以全部六个本机夹角计算一致性残差 $c_{k,S}$。其中三个角用于求解，涉及未入选圆周发射机的另外三个角提供交叉核验。这样可比较同一架接收机在三种参考假设下对完整观测的解释程度。

同时，在理想队形上预先计算各参考对的局部几何指标

$$
\sigma_{k,S}=\sigma_{\min}\!\left(
\left.\frac{\partial(h_{uv})_{\{u,v\}\in\binom{\{0\}\cup S}{2}}}
{\partial(x,y)}\right|_{p=Q_k}
\right).
$$

该指标沿用 1.1 的局部稳定性判断。评分 $\sigma_{k,S}/(1+c_{k,S}/s)$ 同时偏好较好的理想几何和较小的当前观测不一致性；分数相同时按参考对编号排序。参数 $s$ 是评分尺度，与保持阈值分别设置。这个评分是本机选择参考的启发式规则，尚未证明为最优准则；由于六角预测同样使用理想锚点，一致性残差也不直接给出真实位置误差上界。

### 同步相对修正及其耦合影响

每架接收机在同一次调整中同时给出径向与角向指令，以向外和公共旋转正方向为正。仿真精确执行 $r_k^+=r_k+\Delta r_k$、$\theta_k^+=\theta_k+\Delta\theta_k$，再转换为平面位置。所有本机计算使用同一冻结状态；六架完成计算后同步更新，发射机保持不动。

令 $e_{r,k}=\widehat{\delta r}_k-\delta r_k$、$e_{\theta,k}=\widehat{\delta\theta}_k-\delta\theta_k$ 为本次估计误差。在限幅未激活、尚未保持且执行精确时，偏差递推为

$$
\begin{pmatrix}\delta r_k^+\\\delta\theta_k^+\end{pmatrix}
=(1-\eta)\begin{pmatrix}\delta r_k\\\delta\theta_k\end{pmatrix}
-\eta\begin{pmatrix}e_{r,k}\\e_{\theta,k}\end{pmatrix}.
$$

准确估计时两个偏差分量每次按系数 $0.5$ 缩小。实际迭代中，辅助参考位置偏差会影响 $e_{r,k},e_{\theta,k}$，某些时隙的某项误差可能增大。公开轮换让每架待调整机反复接收并移动，也不断改变其后续作为发射机时的参考位置。对这一过程的一般收敛证明需要同时处理参考偏差、择优切换、限幅和保持逻辑；单机准确估计下的缩小关系还不足以完成该证明。

第一时隙由 FY00、FY01、FY02、FY03 发射。六架接收机均选择 FY01/FY03 参考对，用 FY00、FY01、FY03 定位，并用涉及 FY02 的角进行交叉核验。已有动作记录为：

| 接收机 | 估计径向偏差 / m | 估计角向偏差 / ° | 径向指令 / m | 角向指令 / ° |
|:---|---:|---:|---:|---:|
| FY04 | -10.557245 | 4.715237 | 5.000000 | -2.000000 |
| FY05 | -14.808736 | 1.173501 | 5.000000 | -0.586751 |
| FY06 | 2.172592 | -0.974994 | -1.086296 | 0.487497 |
| FY07 | 1.756481 | -0.981173 | -0.878241 | 0.490587 |
| FY08 | -0.421152 | 1.258845 | 0.210576 | -0.629423 |
| FY09 | 13.311707 | 4.587273 | -5.000000 | -2.000000 |

例如 FY04 的实际初始径向偏差为 $5\,\mathrm{m}$，当时估计却为负，体现了尚未准确的辅助参考对估计的影响。其两个补偿量均达到限幅，执行向外 $5\,\mathrm{m}$、负向转过 $2^\circ$；后续时隙根据更新的本机观测继续修正。因此这套方案依靠持续耦合迭代，不能把第一步的估计视为实际偏差真值。

### 本机保持、恢复与停止确认

完整阈值设置为 $\tau_r=10^{-3}\,\mathrm{m}$、$\tau_\theta=10^{-5}\,\mathrm{rad}$、$\tau_c=10^{-5}\,\mathrm{rad}$。本机每次接收后计算核心模型中的 $\rho_k$，并更新自己的计数与保持标志：

1. 尚在调整时，若 $\rho_k\le0.5$，连续达标计数加一；否则清零。累计连续 21 次自身观测达标后进入保持，本次及后续保持时的两个指令均为零。达到 21 次之前仍按增益修正。
2. 保持期间继续测角。若 $\rho_k>1$，清除保持标志和计数，在当前时隙恢复调整；其余情况继续保持。半阈值进入、完整阈值恢复形成滞回区间。
3. 本机作为发射机时保持位置，不新增接收计数，已有记忆延续到下一次自身观测。

每机一个公开周期有 21 次接收，因此任意连续 21 次自身观测覆盖其一个轮换周期中的全部接收参考配置。保持判断完全依赖本机估计、六角残差及自身记忆，其他无人机的状态或评分不参与本机控制。

仿真在固定的完整 28 时隙周期末检查结束条件：只有该周期每一次接收决策都返回保持，才结束日志，状态记为 `quiet_full_cycle`。这项汇总用于确认整段运行已经安静，不向本机控制器反馈其他飞机信息。实际控制器可继续监听，在自身指标超阈值时恢复。默认上限为 20 个周期即 560 个时隙；达到上限仍未满足条件时明确报告未完成停止确认。

### 累计测角时隙下的精度结果

以九架圆周机为评价对象，执行后最大位置误差和均方根误差定义为

$$
E_{\max}(t)=\max_{1\le k\le9}\lVert P_k(t)-Q_k\rVert,
\qquad
E_{\mathrm{rms}}(t)=\sqrt{\frac19\sum_{k=1}^9\lVert P_k(t)-Q_k\rVert^2}.
$$

最大径向误差为 $\max_k|\lVert P_k(t)\rVert-R|$，最大角向误差取实际方位与目标方位的最短角差绝对值；相邻间隔按编号循环计算，包含 FY09 到 FY01。它们都由仿真器根据真实坐标离线计算，不进入本机控制、评分或停止条件。

表 1 默认运行得到如下过程，所有展示值经过舍入：

| 累计时隙 | 最大位置误差 / m | 位置误差 RMS / m | 最大径向误差 / m | 最大角向误差 / ° |
|---:|---:|---:|---:|---:|
| 0 | 12.011140 | 7.414802 | 12.000000 | 0.280000 |
| 28 | 2.153027 | 1.373278 | 1.286538 | 1.156372 |
| 56 | 0.214392 | 0.124854 | 0.109417 | 0.115408 |
| 84 | 0.014876 | 0.008002 | 0.006587 | 0.008009 |
| 112 | 0.00102033 | 0.00049196 | 0.00037597 | 0.00054928 |
| 140 | 0.00007264 | 0.00003743 | 0.00002485 | 0.00003911 |
| 145 | 0.00005180 | 0.00002758 | 0.00002479 | 0.00002807 |
| 196 | 0.00005180 | 0.00002758 | 0.00002479 | 0.00002807 |

第一周期最大角向误差高于初始值，随后记录明显下降，符合参考偏差可能在两种分量间传递的分析。因此不能把这条轨迹解释为每时隙、每项误差均单调减小。

逐行检查完整时隙记录，以 $E_{\max}(t)<\varepsilon$ 作为离线精度判据，首次达标时刻为：

| 精度门槛 $\varepsilon$ | 首次低于门槛的累计时隙 | 该时隙最大位置误差 |
|:---|---:|---:|
| $2\,\mathrm{cm}$ | 80 | $1.78054\,\mathrm{cm}$ |
| $1\,\mathrm{cm}$ | 92 | $0.650561\,\mathrm{cm}$ |
| $1\,\mathrm{mm}$ | 113 | $0.925286\,\mathrm{mm}$ |

对上述三个门槛，首次达到后直到第 196 个时隙的全部后续记录均保持达标。第 145 个时隙发生最后一次非零移动；第 146—196 个时隙位置不再改变，其中第 169—196 个时隙是完整的 28 时隙保持确认周期，在第 196 个时隙结束后停止记录。因此厘米级、毫米级精度达到时刻，最后移动时刻与停止确认时刻应分别报告。

全程共 196 个测角时隙，其中 145 个时隙有移动；六架同时接收累计产生 1176 次本机决策、850 次非零单机动作，局部拟合失败数为 0。末态最大位置误差为 $5.18045\times10^{-5}\,\mathrm{m}$，位置误差 RMS 为 $2.75766\times10^{-5}\,\mathrm{m}$，最大径向误差为 $2.47911\times10^{-5}\,\mathrm{m}$，最大角向误差与最大相邻角间隔误差均约为 $(2.80713\times10^{-5})^\circ$。

这些结果表明，在表 1 初态、FY00/FY01 固定标定、精确测角、公开同步轮换及精确相对极坐标执行条件下，本机迭代达到了上述模型内精度。已有有限增益与初始扰动检查也达到本机停止条件，但仍不构成一般收敛证明。本机保持指标与六角残差不直接等价于真实误差上界；含噪测角、执行误差、更大初始偏差和连续收敛域需要进一步分析。

## 证据与实现来源

1.1 的精确与含噪统计见[定位验证记录](../../docs/q1_1/验证记录.md)，1.2 的候选补查、退化诊断和 560 例检查见[匿名辨识验证记录](../../docs/q1_2/验证记录.md)。1.3 的本机算法与执行假设见[迭代验证记录](../../docs/q1_3/验证记录.md)、[本机控制实现](../../scripts/q1_3/local_adjustment.py)及[仿真实现](../../scripts/q1_3/run_adjustment.py)。累计时隙结果可由[汇总](../../outputs/q1_3/summary.json)、[逐时隙误差](../../outputs/q1_3/error_history.csv)、[本机动作](../../outputs/q1_3/adjustment_steps.csv)和[公开发射表](../../outputs/q1_3/transmitter_schedule.csv)核对。
