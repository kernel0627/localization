# 问题 1（3）迭代参考基线：基于本机双偏差估计的编队调整

针对表 1 的初始队形，目标是使九架圆周无人机恢复到半径 $100\,\mathrm{m}$、相邻角间隔 $40^\circ$ 的理想位置。采用“建立双偏差状态—公开轮换发射—本机择优估计—同步调整”的迭代方法，每架接收机利用自己的夹角观测，估计并修正径向、角向偏差。

## 1. 双偏差状态模型

取 FY00 为原点，FY00 指向 FY01 为横轴正向，理想位置为

$$
Q_0=(0,0),\qquad
Q_k=R\begin{pmatrix}\cos\theta_k^0\\\sin\theta_k^0\end{pmatrix},
\qquad R=100\,\mathrm{m},\quad
\theta_k^0=\frac{2\pi(k-1)}9,\quad k=1,\ldots,9.
$$

针对表 1，FY00、FY01 恰处于目标位置，因此将其作为已标定固定基准。FY02—FY09 的实际位置写成

$$
P_k=(R+\delta r_k)
\begin{pmatrix}
\cos(\theta_k^0+\delta\theta_k)\\
\sin(\theta_k^0+\delta\theta_k)
\end{pmatrix}.
$$

其中 $\delta r_k$ 和 $\delta\theta_k$ 分别为径向、角向偏差，调整目标是使两者同时接近零。每架无人机已知自身编号、理想队形和公共时隙，利用本机夹角及自身历史状态作出决策。假设无人机可根据 FY00 的来向建立局部径向，并按照统一旋转方向精确完成相对径向位移与角向位移。

## 2. 公开发射轮换模型

将 FY02—FY09 的全部二元组合 $(a,b)$ 按编号字典序循环排列，每个时隙的发射集合及圆周发射集合分别为

$$
T=\{0,1,a,b\},\qquad C=\{1,a,b\},\qquad 2\leq a<b\leq9.
$$

FY00、FY01 和两架辅助机保持位置并发射，其余六架接收。每次先固定队形完成测角，再由各接收机独立计算、同时执行修正，随后进入下一时隙。每架接收机获得四个发射源之间的六个无向夹角，记为 $\alpha_{uv,k}^{\mathrm{obs}}$，角度计算统一采用弧度。

**一个测角时隙就是一次“测角—本机计算—同步执行”。** 四架同时发射、六架同时调整合计仍为一个时隙；继续测角但保持位置也计入时隙。累计时隙 $t=0$ 表示初始状态，第 $t$ 个时隙的误差按该次执行后的队形计算。

轮换表含 $\binom82=28$ 个时隙，每架待调整机发射 7 次、接收 21 次。28 时隙用于组织公开顺序与保持确认，调整效率以达到给定精度所需的累计时隙衡量。题目未规定单时隙时长，因此时隙数表示操作次数，尚不能换算为实际耗时。

## 3. 本机参考择优与双偏差估计

沿用已知发射位置下的角度观测方程，对候选接收位置 $p$ 定义

$$
h_{uv}(p)=\arccos\frac{(Q_u-p)^{\mathsf T}(Q_v-p)}
{\lVert Q_u-p\rVert\,\lVert Q_v-p\rVert}.
$$

辅助发射机尚有偏差时，观测由其实际位置产生，模型仍以编号对应的理想位置作为参考，因此本机估计会受到参考偏差的影响。每架接收机分别尝试 $S\in\{\{1,a\},\{1,b\},\{a,b\}\}$，每对与 FY00 构成三个定位发射点。

用 $z=(z_r,z_t)=(\widehat{\delta r},R\widehat{\delta\theta})$ 将两个估计变量统一到米的尺度，候选位置及局部最小二乘模型为

$$
\begin{aligned}
p_k(z)&=(R+z_r)
\begin{pmatrix}
\cos(\theta_k^0+z_t/R)\\
\sin(\theta_k^0+z_t/R)
\end{pmatrix},\\
z_{k,S}^*&=\underset{z}{\operatorname{argmin}}
\sum_{\{u,v\}\in\binom{\{0\}\cup S}{2}}
\left[h_{uv}(p_k(z))-\alpha_{uv,k}^{\mathrm{obs}}\right]^2.
\end{aligned}
$$

各参考对均从零偏差出发求取附近的解。三个夹角用于拟合，涉及第三架圆周发射机的另外三个夹角用于交叉核验。对每个有效候选，定义六角一致性残差与几何指标

$$
c_{k,S}=
\left[\sum_{\{u,v\}\in\binom T2}
\left(h_{uv}(p_k(z_{k,S}^*))-\alpha_{uv,k}^{\mathrm{obs}}\right)^2\right]^{1/2},
\qquad
\sigma_{k,S}=\sigma_{\min}\bigl(J_{k,S}(Q_k)\bigr).
$$

其中 $J_{k,S}$ 为该参考对三个预测夹角对笛卡尔位置的 Jacobian，在理想队形处计算；最小奇异值反映局部定位几何的稳定性。综合两项指标，选择

$$
S_k^*=\underset{S\text{ 为有效候选}}{\operatorname{argmax}}
\frac{\sigma_{k,S}}{1+c_{k,S}/s},
\qquad
\widehat{\delta r}_k=z_{r,k,S_k^*}^*,
\quad
\widehat{\delta\theta}_k=\frac{z_{t,k,S_k^*}^*}{R}.
$$

分数相同按参考编号顺序选择。该评分兼顾理想几何与本机完整观测的一致性，是参考选择的启发式规则；全部计算均在接收机本地完成。评分尺度 $s$ 与保持阈值分别设置。

## 4. 同步调整与停止规则

定义 $\operatorname{clip}(x,l,u)=\min(\max(x,l),u)$。选定参考后，接收机同时给出两项相对修正：

$$
\begin{aligned}
\Delta r_k&=\operatorname{clip}(-\eta\widehat{\delta r}_k,-d_r,d_r),&
r_k^+&=r_k+\Delta r_k,\\
\Delta\theta_k&=\operatorname{clip}(-\eta\widehat{\delta\theta}_k,-d_\theta,d_\theta),&
\theta_k^+&=\theta_k+\Delta\theta_k.
\end{aligned}
$$

增益控制修正幅度，限幅约束单次运动。若估计准确且限幅未触发，在调整状态下两种偏差都按系数 $1-\eta$ 缩小；辅助参考的位置偏差会影响估计，某项误差可能暂时增大，需要持续轮换和重新测角。

本机以选中候选的估计与六角残差计算

$$
\rho_k=\max\left(
\frac{|\widehat{\delta r}_k|}{\tau_r},
\frac{|\widehat{\delta\theta}_k|}{\tau_\theta},
\frac{c_{k,S_k^*}}{\tau_c}
\right).
$$

连续 21 次自身接收观测满足 $\rho_k\leq0.5$ 后进入保持，指令置零；未达标则重新计数，计数期间继续修正。21 次连续接收覆盖本机一个轮换中的全部接收配置。保持后继续测角，若 $\rho_k>1$，在当前时隙恢复调整。发射时保持当前位置，接收计数延续。

仿真在一个完整、与轮换表对齐的 28 时隙窗口中，全部接收决策均为保持后结束记录。该条件汇总控制状态用于停止确认；本机控制仍依据自身观测与记忆。

| 参数 | 默认值 | 含义 |
|:---|:---|:---|
| $\eta$ | $0.5$ | 修正增益 |
| $d_r,\ d_\theta$ | $5\,\mathrm{m},\ 2^\circ$ | 单时隙径向、角向限幅 |
| $s$ | $10^{-3}\,\mathrm{rad}$ | 参考评分尺度 |
| $\tau_r$ | $10^{-3}\,\mathrm{m}$ | 径向估计完整阈值 |
| $\tau_\theta,\ \tau_c$ | 均为 $10^{-5}\,\mathrm{rad}$ | 角向估计、六角残差完整阈值 |
| 连续达标次数 | 21 次本机接收 | 使用半阈值进入保持 |

## 5. 表 1 的累计时隙结果

仿真器用实际坐标生成观测、模拟执行并离线评价误差。九架圆周机的最大位置误差和均方根误差定义为

$$
E_{\max}(t)=\max_{1\leq k\leq9}\lVert P_k(t)-Q_k\rVert,\qquad
E_{\mathrm{rms}}(t)=
\sqrt{\frac19\sum_{k=1}^9\lVert P_k(t)-Q_k\rVert^2}.
$$

默认参数下的轨迹如下，数值取自[逐时隙误差记录](../../outputs/q1_3/iterative_reference_baseline/error_history.csv)。

| 累计测角时隙 | 最大位置误差 / m | 位置误差 RMS / m |
|---:|---:|---:|
| 0 | 12.011140 | 7.414802 |
| 28 | 2.153027 | 1.373278 |
| 56 | 0.214392 | 0.124854 |
| 84 | 0.014876 | 0.008002 |
| 112 | 0.00102033 | 0.00049196 |
| 140 | 0.00007264 | 0.00003743 |
| 145 | 0.00005180 | 0.00002758 |
| 196 | 0.00005180 | 0.00002758 |

逐行检索完整记录，可将精度进展、结束移动和停止确认分别列为：

| 累计测角时隙 | 事件 | 该时隙最大位置误差 |
|---:|:---|---:|
| 80 | 首次低于 $2\,\mathrm{cm}$ | $1.78054\,\mathrm{cm}$ |
| 92 | 首次低于 $1\,\mathrm{cm}$ | $0.650561\,\mathrm{cm}$ |
| 113 | 首次低于 $1\,\mathrm{mm}$ | $0.925286\,\mathrm{mm}$ |
| 145 | 最后一次非零移动 | $0.0518045\,\mathrm{mm}$ |
| 196 | 完成停止确认 | $0.0518045\,\mathrm{mm}$ |

三项精度门槛首次达到后，后续记录均保持达标。第 146～196 个时隙没有位置改变，其中第 169～196 个时隙构成完整停止确认窗口。因此，精度效率应报告为达到厘米级所需的 92 个时隙、达到毫米级所需的 113 个时隙；196 个时隙包含了保持确认开销。精度门槛用于离线评价，本机按第 4 节的规则进入保持。

本次共发生 850 次非零单机动作，分布在 145 个测角时隙内；共有 1176 次本机决策，局部拟合失败数为零。最终最大径向误差约为 $2.47911\times10^{-5}\,\mathrm{m}$，最大角向误差约为 $(2.80713\times10^{-5})^\circ$。早期最大角向误差曾从 $0.28^\circ$ 增至第 28 时隙的 $1.156372^\circ$，体现了参考偏差的耦合影响，不能将轨迹理解为逐时隙单调下降。

已有[敏感性结果](../../outputs/q1_3/iterative_reference_baseline/sensitivity.csv)中，表 1 在增益 $0.25,\ 0.5,\ 1$ 下分别于累计 224、196、196 时隙完成停止确认，末态最大位置误差分别约为 $1.52\times10^{-4}$、$5.18\times10^{-5}$、$9.05\times10^{-6}\,\mathrm{m}$；三组额外笛卡尔扰动均于 196 时隙完成确认，末态最大误差处于 $5.70\times10^{-5}$ 至 $7.46\times10^{-5}\,\mathrm{m}$。理想初态保持零位置误差，于 56 时隙完成确认。这些有限案例均无局部拟合失败。

结果适用于固定已标定 FY00/FY01、精确测角和精确相对极坐标执行的仿真条件。六角一致性与本机保持指标尚未给出真实误差的上界，现有算例也未证明一般收敛性或含噪条件下的精度。

## 6. 复现与验证资料

新增[局部收敛与统一评价](main局部收敛与评价.md)：持续修正核心在默认增益下的 28 时隙周期谱半径约为 $0.079220$，已用原求解器微扰核验。该结论限定于目标附近、正确局部解、未限幅且未保持的反馈映射；完整有限阈值程序仍需区分保持记忆。表 1 默认运行完整计入 784 个发射机次、约 254.234 m 累计端点位移，其中最后移动后的确认占 204 个发射机次。详细逐时隙记录与参数评价保存在 `outputs/q1_3/main_analysis/`。

在仓库根目录运行：

    python -m pip install -r requirements.txt
    python -m scripts.q1_3.run_iterative_reference_baseline

模型实现见 [local_adjustment.py](../../scripts/q1_3/local_adjustment.py)，仿真入口见 [run_adjustment.py](../../scripts/q1_3/run_iterative_reference_baseline.py)。求解器设置、候选有效性检查及测试记录见[验证记录](迭代参考基线验证.md)。

主要结果包括[汇总参数与误差](../../outputs/q1_3/iterative_reference_baseline/summary.json)、[公开轮换表](../../outputs/q1_3/iterative_reference_baseline/transmitter_schedule.csv)、[参考候选](../../outputs/q1_3/iterative_reference_baseline/local_candidates.csv)和[逐机调整日志](../../outputs/q1_3/iterative_reference_baseline/adjustment_steps.csv)。完整的第一问推导见[第一问整体解法](../../solutions/q1/README.md)。
